<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tool="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:background="@color/colorPrimaryDark">

    <LinearLayout
        android:layout_width="match_parent"
        android:id="@+id/widget_main_bar"
        android:layout_height="?android:attr/actionBarSize"
        android:background="@color/colorPrimary">

        <TextView
            android:id="@+id/widget_name"
            android:layout_width="@dimen/widget_stock_layout_width"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:layout_marginStart="@dimen/widget_stock_header_margin"
            android:layout_marginEnd="@dimen/widget_stock_header_margin"
            android:gravity="center_vertical"
            android:textColor="@color/white"
            tool:text="@string/app_name"
            android:textAppearance="@android:style/TextAppearance.DeviceDefault.Widget.ActionBar.Title"/>
    </LinearLayout>

    <ListView
        android:id="@+id/widget_listView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"></ListView>

</LinearLayout>